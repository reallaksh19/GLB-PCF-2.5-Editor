/**
 * Keyboard shortcuts module.
 *
 * Registers keyboard shortcuts for common editing operations. In the
 * static editor prototype this module does not register listeners
 * automatically; a hosting application may call `bindShortcuts()` to
 * wire the callbacks. All callbacks are no-ops in this stub.
 */

/**
 * Bind keyboard shortcuts. In a real implementation this would
 * register event listeners on the document. This stub does nothing.
 */
export function bindShortcuts() {
  // Intentionally empty.
}