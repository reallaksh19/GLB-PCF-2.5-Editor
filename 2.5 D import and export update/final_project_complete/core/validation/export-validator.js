/*
 * core/validation/export-validator.js
 *
 * Placeholder for export validation.  Later waves may verify that
 * the graph is ready for export (e.g. no proxy entities remain).
 */

export function validateExport(graph) {
  return { errors: [], warnings: [] };
}